# 公众号推文封面生成计划

## 目标
为《无需精细标注的语义引导医学图像增广：让“造”出来的数据依然可信》生成一张可直接用于微信公众号的封面图，并保存到当前项目。

## 阶段
- [x] 读取 PDF，提炼主题、论点和可视化元素
- [x] 确认视觉方向与封面文案层级
- [x] 生成封面并检查构图、文字与气质
- [x] 保存成品并交付

## 补充变体
- [x] 生成“三步演化”横向封面
- [x] 生成“分布搜索”横向封面
- [x] 检查并保存两张补充封面

## 当前决策
- 默认按公众号头图的横向构图设计。
- 科研传播感优先：专业、清晰、有医学影像辨识度，避免泛化的“科技蓝背景”。

## 错误记录
- 首次文本提取遇到 PowerShell GBK 无法输出箭头字符；改用 `PYTHONIOENCODING=utf-8` 后成功。
- bundled `pdftoppm.cmd` 首次渲染报“找不到指定路径”；将检查包装脚本并改用实际二进制路径或 Python 渲染。
- 已定位实际 Poppler 可执行文件并成功渲染前 3 页。
- 计划自检命令因 `git diff --no-index` 检测到文件存在差异而返回状态码 1；这是 diff 的正常语义，非计划内容错误。占位符扫描无命中，需求覆盖完整。
